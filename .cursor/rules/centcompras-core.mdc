---
description: CentCompras core architecture, scope, and development pace
alwaysApply: true
---

# CentCompras core

## Project

Central warehouse + satellite branches. Django 6.1, PostgreSQL, plain HTML/JS. Phases 0–5 are done: catalogue + pricing + purchase orders + goods receipt + manager catalog + branches/requisição/goods issue/branch receipt. Offline and email are later (deferred/pending).

## What exists today

- `accounts` — custom User (email, timezone), warehouse groups, login
- `products` — catalogue + selling prices + `SupplierItemPrice`, staff console `/manage/items/`, manager catalog `/manage/catalog/` (read-only stock + prices)
- `procurement` — purchase orders at `/manage/purchase-orders/`
- `inventory` — goods receipt + `StockMovement` ledger + cached `Item.quantity` at `/manage/goods-receipts/`; goods issue at `/manage/internal-requests/`; branch receipt + branch stock ledger (`BranchStockMovement` + `BranchItemStock`)
- `logging_utils` — rotating logs in `logs/`
- `branches` — tenancy (`Branch`, `BranchMembership`, picker, `/branch/catalog/`) — **built** (Phase 5 ✅)
- `orders` — internal request (requisição interna) — **built** (Phase 5 ✅)
- Offline catalogue — **removed** (Phase 7 may re-add it)

## Non-negotiables

- PostgreSQL = source of truth. No client-side writes.
- Business logic in `services.py`; views/commands stay thin
- Warehouse groups (`warehouse_admins`, `warehouse_managers`, `warehouse_data_operators`); `/admin/` is superuser only
- Plain Django + plain JavaScript — no React/Vue
- Incremental phases — no large application dumps
- Do not implement the tenancy-doc `Order` stub (`item_name`)
- Do not start offline, email, or shared chrome in passing (Phase 5 is done)
- Do not restyle `/` or polish the staff console unless that session is requested

## References

- @docs/handoff.md — **read first** (state, decisions, next task)
- @README.md — setup
- @docs/PROJECT-PLAN.md — living plan: sequencing + status tracker + locked decisions (update every session)
- @docs/archive/code-review-full-2026-08-21-1303.md — concluded 1303 review (N1–N12 applied; archived)
- @docs/archive/code-review-full-2026-08-20-2208.md — concluded 2208 review (do not re-open)
- @docs/archive/phase5-brainstorm-260821-1530.md — Phase 5 locked decisions (archived); @docs/archive/warehouse-tenancy-setup.md — archived sketch only
