# --- Django ---
# Which settings module to use (default: dev). Production sets config.settings.prod
# DJANGO_SETTINGS_MODULE=config.settings.dev

# --- Secrets (REQUIRED in production) ---
# SECRET_KEY=change-me-in-production
# DEBUG=True

# --- Hosts ---
# Comma-separated. Production must set this (e.g. yourdomain.com,www.yourdomain.com)
# ALLOWED_HOSTS=localhost,127.0.0.1
# CSRF_TRUSTED_ORIGINS=https://yourdomain.com

# --- Google OAuth login (login-only scopes: openid, email, profile) ---
# AUTH_MODE: both (default; dev + initial prod) | google_only (final prod,
# password login disabled — Google is the only method)
# AUTH_MODE=both
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# Desktop app client during dev (loopback):
# GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/accounts/google/callback/
# Web application client once deployed:
# GOOGLE_OAUTH_REDIRECT_URI=https://yourdomain.com/accounts/google/callback/

# --- Database ---
# Local dev:  DATABASE_URL=postgresql://appuser:your_password_here@localhost:5432/centcompras_db
# Production: DATABASE_URL=postgresql://centcompras:STRONG_PASSWORD@localhost:5432/centcompras_db
# DATABASE_URL=

# POSTGRES_* vars remain a supported fallback for local dev when DATABASE_URL is unset:
# POSTGRES_USER=appuser
# POSTGRES_PASSWORD=your_password_here
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
